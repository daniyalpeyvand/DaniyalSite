@model IEnumerable<DataLayer.PageComment>

@foreach (var item in Model)
{
	<li>
		<img src="~/Content/UI/img/avatar.jpg">
		<div>
			<span> @item.Name </span>
			<span> @item.CreateDate.ToShamsi() </span>
		</div>
		<p>@item.Comment</p>
	</li>
}
